create practice tests based on the material in a given classroom 
  - Use the response generated by the LLM to create questions and tests.
allows student to ask questions to an LLM and LLM is given context from the material in GC
  - like an ai chatbox but specifically for the assignment that the user is referring to.
  - vectorize the gc assignment that the user is referring to and then vectorize the question the user has
  - use Ollama to generate a response.